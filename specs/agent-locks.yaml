# specs/agent-locks.yaml — story locks (e39s02).
# Acquire before running tests for a story; release when the story lands.
# 2026-09-20: e02s01 lock released — plan frozen by user before develop-tdd
# (see specs/product/snapshots/release-0.6.0/FROZEN.md). Re-acquire on unfreeze.
# 2026-09-20 step 8 (release-branch): verified released at story close — no lock is
# held for e02s01 and none needs releasing; re-acquire if e02s01 reopens. e02s02
# acquires its own lock at kickoff.
# 2026-09-20T07:37Z: e02s02 cycle opened by orchestrator — lock acquired. Step 3 (kickoff-branch,
# 2026-09-20T07:55Z): confirmed held and delegated to the resident story_ops child; locked_at kept.
# 2026-09-20T08:33Z step 8 (release-branch): e02s02 lock RELEASED at story close — the branch is
# pushed for a PR decision and this cycle holds no writer. Re-acquire if e02s02 reopens (e.g. a
# landing-time reconcile of specs/state.yaml against the advanced origin/main).
# 2026-09-20T08:41Z: e02s03 cycle opened by orchestrator — lock acquired.
# 2026-09-20T09:1xZ step 3 (kickoff-branch): confirmed held and delegated to the resident
# story_ops child — `agent` updated to story_ops, `locked_at` kept (orchestrator's acquire time).
# 2026-09-20T09:56Z step 8 (release-branch, team-pr decision mode): e02s03 lock RELEASED at story
# close — the branch is pushed for the PR decision and this cycle holds no writer. That is the last
# story of epic e02, so re-acquire (a new lock) only if the story reopens — e.g. if the landing-time
# reconcile must resolve specs/state.yaml against the advanced origin/main.
# 2026-09-24T10:42Z: e03s01 completed via delegated writer (orchestrator-verified preflight
# 1039 passed/5 skipped/88 subtests, ruff clean) — the delegation bootstrap predated the lock;
# discipline re-established at e03s02. e03s01 needs no release (done, unlanded on feat/e03).
# 2026-09-24T10:42Z: e03s02 cycle opened by orchestrator — lock acquired ahead of the
# delegated develop-tdd run (branch-only mode: feat/e03 checked out in the main repo dir).
# 2026-09-24T11:1xZ: e03s02 TDD cycle closed by orchestrator (preflight 1046/5/88, ruff clean;
# ledger header flip applied post-run) — lock RELEASED; story unlanded on feat/e03 like e03s01.
# Same timestamp: e03s03 cycle opened — lock acquired ahead of the delegated develop-tdd run.
locks:
  - story_id: e03s03
    locked_by: 'agent: orchestrator (delegated writer for e03s03)'
    locked_at: '2026-09-24T11:10:00Z'
    files_touched: []
