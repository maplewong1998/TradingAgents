# specs/metrics/cycle-times.yaml — per-story cycle-time ledger.
#
# Appended by release-branch when a story lands:
#   bcp_per_hour = story_bcps / cycle_minutes * 60
# Sourced from git commit history via `bash scripts/record-cycle-time.sh`.
#
# 2026-09-20 (e02s01): `record-cycle-time.sh append` writes its OKF story-metrics
# bundle INTO this file, which made it a 2-document stream and broke
# `scripts/validate-specs-yaml.sh` ("expected a single document in the stream").
# The bundle now lives in specs/metrics/e02s01-story-metrics.okf.md — its canonical
# OKF shape (YAML frontmatter + markdown body, per the script's own pointer to
# specs/templates/story-metrics.okf.md) — so this ledger stays one document.
# scripts/ is a symlink to the global bigpowers install: never edited, so the tool
# defect is logged rather than patched (see the step-8 record in progress.md).

stories:
  # 2026-09-20 e02s01 — written by build-epic step 8 (release-branch → landed on main).
  # Row updated after the landing commit adopted a `Story: e02s01` trailer: the trailer
  # works (record-cycle-time.sh no longer reports this story as `unattributed`), but the
  # git-hours effort degenerates to 0.0 h because the landing was a SQUASH — one commit
  # with a 0-min coding span, so no inter-commit interval is left to measure. The
  # wall-clock cycle time therefore stays the operative value; the OKF bundle below is
  # the script's own output (2-document YAML: list row + OKF story-metrics document).
  - id: e02s01
    epic: e02
    bcps: 8
    story_start: '2026-09-20T04:21:39Z'
    story_end: '2026-09-20T05:35:10Z'
    cycle_minutes: 73.5          # wall-clock story_start → landing write
    bcp_per_hour: 6.53           # 8 bcps / 73.5 min * 60 — wall-clock basis
    effort_hours: 0.0            # git-hours via trailer: degenerate on the squash
    attributed_by_trailer: true  # `Story: e02s01` on d7557e4
    commits: 13                  # story range 114f769..d96645a on the branch (4 more pre-story)
    merge_commits: 1             # d7557e4 squash on main
    branch: feat/e02-conditional-debate-gate
    landed: true
    merge_ref: d7557e4
    source: wall-clock           # cycle_minutes; the git-effort column is 0.0 (see note)
    audit_score_pct: 100         # gate round 2, delta-only, specs/verifications/AUDIT-e02-e02s01.md
    verify_status: pass          # specs/verifications/e02s01-verify.yaml, 7/7 phases
    rework_count: 2              # develop round 2 + gate round 2 (both PASS)

  # 2026-09-20 e02s02 — written by build-epic step 8 (release-branch, team-pr decision mode, then
  # LANDED on the user's Safety-gate choice). Both cycle bases are labelled and both are claimed:
  # cycle_minutes is wall-clock (story_start → step-8 write), effort_hours is git-derived via the
  # `Story: e02s02` trailers. The user chose a MERGE-COMMIT landing precisely so git-effort survives
  # — e02s01's squash measured a 0-min coding span and degenerated to 0.0 h. `record-cycle-time.sh
  # report --range 3bb54bd..HEAD` gives e02s02 0.77 h / 19 commits / 47 min coding span, additivity
  # PASS (1 unattributed commit, 0.02 h — the merge/cockpit commits).
  - id: e02s02
    epic: e02
    bcps: 5
    story_start: '2026-09-20T07:37:31Z'
    story_end: '2026-09-20T08:33:00Z'
    cycle_minutes: 55.5          # wall-clock story_start → step-8 write
    bcp_per_hour: 5.41           # 5 bcps / 55.5 min * 60 — wall-clock basis
    effort_hours: 0.77           # git-hours via trailers (merge-commit landing; additivity PASS)
    lead_time_minutes: 47        # coding span reported for the story range
    attributed_by_trailer: true  # `Story: e02s02` on the story commits and the merge commit
    commits: 19                  # story range 06d984e..2333a1b: 17 develop/verify/gate + 2 step-7/8 hygiene
    merge_commits: 1             # 71bf36b — MERGE COMMIT, main parents 3bb54bd + 2333a1b
    branch: feat/e02s02
    landed: true
    merge_ref: 71bf36b
    source: wall-clock + git-hours   # cycle_minutes = wall-clock; effort_hours/lead_time = git-derived
    audit_score_pct: 100         # gate round 1, full delta, specs/verifications/AUDIT-e02-e02s02.md
    verify_status: pass          # specs/verifications/e02s02-verify-r2.yaml, 9/9 phases (round 1 fail -> round 2 pass)
    rework_count: 3              # develop round 2 + verify round 2 + quick-fix BUG during round 2
    routed_open: 'tests/_tmp_cache gitignore entry; CONVENTIONS.md cli/main.py cap row stale (1460 -> 1276); double env notice (cli/gate_policy.py:76-81, :99-104) — all to e02s03'

  # 2026-09-20 e02s03 — written by build-epic step 8 (release-branch, team-pr decision mode), then
  # LANDED on the user's Safety-gate choice. Both cycle bases are labelled and both are claimed:
  # cycle_minutes is wall-clock (story_start → step-8 write, the e02s01/e02s02 basis), effort_hours is
  # git-derived from the `Story: e02s03` trailers. The user chose a MERGE COMMIT, which preserves the
  # per-commit history: `record-cycle-time.sh report --range 37dbc93..HEAD` gives e02s03 0.98 h /
  # 23 commits / 59 min coding span, ADDITIVITY PASS against the whole-range oracle (0.9792 h) — the
  # landing did what e02s01's squash could not (0.0 h). The 24th commit is this landing record, whose
  # trailer lands after the measurement, so the row stays at the measured figures.
  - id: e02s03
    epic: e02
    bcps: 3
    story_start: '2026-09-20T08:41:06Z'
    story_end: '2026-09-20T09:56:34Z'
    cycle_minutes: 75.5          # wall-clock story_start → step-8 write
    bcp_per_hour: 2.39           # 3 bcps / 75.5 min * 60 — wall-clock basis
    effort_hours: 0.98           # git-hours via the `Story: e02s03` trailers (merge-commit landing)
    lead_time_minutes: 59        # coding span reported for the story range
    attributed_by_trailer: true  # `Story: e02s03` on the story commits and the merge commit
    commits: 23                  # as measured by record-cycle-time.sh for 37dbc93..HEAD; the landing record is the 24th
    merge_commits: 1             # 26f3916 — MERGE COMMIT, main parents 37dbc93 + 8d930cc
    branch: feat/e02s03
    landed: true
    merge_ref: 26f3916
    source: wall-clock + git-hours   # cycle_minutes = wall-clock; effort_hours/lead_time = git-derived
    audit_score_pct: 100         # gate rounds 1 + 2 both 100/100 (round 2 docs-only), specs/verifications/AUDIT-e02-e02s03.md
    verify_status: pass          # specs/verifications/e02s03-verify-r1.yaml, 9/9 phases
    rework_count: 2              # develop round 2 (docs fix-forward) + gate round 2 (both PASS)
    landed_via: 'step 8 — MERGE COMMIT 26f3916 on main (parents 37dbc93 + 8d930cc), pushed 37dbc93..26f3916;
      the user''s Safety gate chose a local merge commit over the team-pr squash default (gh absent).
      Worktree .worktrees/e02s03 removed, branches kept (CI unverified: gh absent — accepted by the user)'
    rework_notes: 'verify round 1 PASS 9/9; gate rounds 1+2 both PASS 100/100 (round 2 docs-only)'
    routed_open: 'post-e02: injectable reachability check (fix-bug, BUG-2026-09-20-no-data-handling-live-vendor-probe);
      check-import-boundaries tooling gap (specs/import-boundaries.json absent); e01s01/e01s02 trace-stale items;
      CHANGELOG "Gate decisions log at INFO" wording nit for the judged-hold branch'
