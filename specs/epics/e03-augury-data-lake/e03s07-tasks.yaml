story_id: e03s07
title: "Cross-sectional context, analyst-bound: liquidity + feature vector (market), universe membership (fundamentals)"
spec: e03s07-cross-sectional-context.md
status: failing
bcps: 5
# TDD ledger (e45s06): every task starts `failing`; flip to `passing` only after
# its verify command exits 0.
tasks:
  - id: 1
    description: >-
      RED: extend tests/test_augury_tools.py (# story: e03s07) — liquidity
      markdown + the endpoint's vintage answer pinned from openapi.json
      (withholding rule if live-only); feature vector with a per-item `failed`
      slot rendered explicitly; universe membership present / delisted / absent
      trichotomy; binding-gate on/off matrix; PM has no ToolNode.
      Scenarios SC-e03s07-P0-01, P1-01/02/03.
    verify: ".venv/bin/python -m pytest -q tests/test_augury_tools.py -k 'liquidity or feature_vector or universe' --collect-only"
    risk: P1
    status: failing
    allure:
      severity: high
      categories: ["unit", "augury", "tools", "RED"]
  - id: 2
    description: >-
      GREEN vendor: get_augury_liquidity (GET /liquidity/{ticker}),
      get_augury_feature_vector (POST /api/v1/batch/feature-vector, one-ticker
      body, 1–500 contract, failed slot → explicit "not available: <reason>"
      line), get_augury_universe_membership (GET /api/v1/universe?as_of=&q=,
      delisted included → present/delisted/absent trichotomy). requests.post
      joins requests.get at the same _request-mapped boundary.
    verify: ".venv/bin/python -m pytest -q tests/test_augury_tools.py -k 'vendor and (liquidity or feature_vector or universe)'"
    risk: P1
    status: failing
    allure:
      severity: high
      categories: ["unit", "augury", "cross-sectional", "PIT"]
  - id: 3
    description: >-
      Seam: cross_sectional category added to TOOLS_CATEGORIES +
      VENDOR_METHODS + OPTIONAL_CATEGORIES (fail-open enrichment;
      interface.py pattern).
    verify: ".venv/bin/python -m pytest -q tests/test_augury_tools.py -k 'registration or routing'"
    risk: P1
    status: failing
    allure:
      severity: high
      categories: ["unit", "augury", "routing", "fail-open"]
  - id: 4
    description: >-
      GREEN tools + bindings: new agents/utils/cross_sectional_tools.py
      (@tool get_liquidity / get_feature_vector / get_universe_membership with
      InjectedState("trade_date") + as_of clamp); agent_utils.__all__ exports;
      _create_tool_nodes appends get_liquidity + get_feature_vector to the
      market node and get_universe_membership to the fundamentals node under
      the binding gate; conditional prompt paragraphs; test asserts the graph
      builds with no Portfolio Manager ToolNode and an unchanged node/edge set.
    verify: ".venv/bin/python -m pytest -q tests/test_augury_tools.py -k 'cross_sectional or binding or prompt'"
    risk: P0
    status: failing
    allure:
      severity: critical
      categories: ["unit", "augury", "binding-gate", "graph-integrity"]
  - id: 5
    description: "Full Preflight."
    verify: ".venv/bin/python -m pytest -q && ruff check ."
    risk: P1
    status: failing
    allure:
      severity: high
      categories: ["preflight"]
